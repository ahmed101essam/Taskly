doctype html
html
  head
    meta(charset="UTF-8")
    meta(name="viewport" content="width=device-width, initial-scale=1.0")
    title Project Invitation
    style.
      body {
        font-family: Arial, sans-serif;
        background-color: #f4f4f4;
        margin: 0;
        padding: 20px;
        text-align: center;
      }
      .container {
        background-color: #ffffff;
        padding: 20px;
        border-radius: 8px;
        box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.1);
        max-width: 500px;
        margin: auto;
      }
      h2 {
        color: #333;
      }
      p {
        color: #555;
        font-size: 16px;
      }
      .btn {
        display: inline-block;
        padding: 10px 20px;
        background-color: #007bff;
        color: #ffffff;
        text-decoration: none;
        font-weight: bold;
        border-radius: 5px;
        margin-top: 20px;
      }
  body
    .container
      h2 You’ve Been Invited to Join #{projectName}!
      p Hello #{userName},
      p #{inviterName} has invited you to collaborate on the project <strong>#{projectName}</strong>.
      p Click the button below to accept the invitation:
      a.btn(href="#{invitationLink}") Accept Invitation
      p If you didn’t expect this invitation, you can ignore this email.
